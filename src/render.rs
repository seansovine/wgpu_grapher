use crate::render_state::RenderState;

pub fn render(_state: &mut RenderState) -> Result<(), wgpu::SurfaceError> {
  // TODO: Think about what data we need for rendering.
  //       Maybe a vector of "mesh" objects.

  Ok(())
}
